{
  "name": "@pdf-studio/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PDF Studio — API Fastify : orchestration des workers Python (structurization + rendu PDF)",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^9.0.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.19.2",
    "typescript": "~6.0.2"
  }
}
